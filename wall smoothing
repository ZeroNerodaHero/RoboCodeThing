BattlefieldSpecification  map = getBattlefield();


if(getX() + 10 >= map.getWidth()) moveDirection *= -1;
if(getX() - 10 >= 0) moveDirection *= -1;
if(getY() + 10 >= map.getHeight()) moveDirection *= -1;
if(getY() - 10 >= 0) moveDirection *= -1;
